# IP asli pengunjung jika request lewat Cloudflare (header CF-Connecting-IP).
# Tanpa ini, limit_req memakai IP edge Cloudflare sehingga throttling tidak per klien.
map $http_cf_connecting_ip $limit_client_ip {
    ""      $binary_remote_addr;
    default $http_cf_connecting_ip;
}

# Rate limiting zones — sedikit lebih longgar agar NAT/kampus tidak false positive (sesuaikan di VPS kecil jika perlu)
limit_req_zone $limit_client_ip zone=api:10m rate=9r/s;
limit_req_zone $limit_client_ip zone=berita_html:10m rate=5r/s;
limit_req_zone $limit_client_ip zone=site_root:10m rate=18r/s;
limit_req_zone $limit_client_ip zone=login:10m rate=1r/s;

# Batasi koneksi paralel ke upstream per IP (kurangi tab+script yang menembak Mongo lewat Node)
limit_conn_zone $limit_client_ip zone=conn_per_ip:10m;
limit_conn_status 503;

# HTTP to HTTPS redirect
server {
    listen 80;
    server_name himatif-encoder.com www.himatif-encoder.com;
    return 301 https://$server_name$request_uri;
}

# HTTPS server
server {
    listen 443 ssl http2;
    server_name himatif-encoder.com www.himatif-encoder.com;

    # SSL configuration
    ssl_certificate /etc/letsencrypt/live/himatif-encoder.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/himatif-encoder.com/privkey.pem;
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers ECDHE-RSA-AES256-GCM-SHA512:DHE-RSA-AES256-GCM-SHA512:ECDHE-RSA-AES256-GCM-SHA384:DHE-RSA-AES256-GCM-SHA384;
    ssl_prefer_server_ciphers off;
    ssl_session_cache shared:SSL:10m;
    ssl_session_timeout 10m;

    # Security headers
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-XSS-Protection "1; mode=block" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;
    add_header Content-Security-Policy "default-src 'self'; script-src 'self' 'unsafe-inline' 'unsafe-eval'; style-src 'self' 'unsafe-inline'; img-src 'self' data: https:; font-src 'self' data:; connect-src 'self' https:; media-src 'self' data: blob: https://*.googleusercontent.com https://drive.google.com https://*.google.com https://*.googlevideo.com; frame-ancestors 'self';" always;
    add_header Strict-Transport-Security "max-age=31536000; includeSubDomains; preload" always;
    add_header Permissions-Policy "geolocation=(), microphone=(), camera=()" always;

    # File upload size limit (untuk file upload yang besar)
    client_max_body_size 100M;
    
    # Timeout settings untuk upload file besar (10 menit)
    client_body_timeout 600s;
    client_header_timeout 600s;
    proxy_connect_timeout 600s;
    proxy_send_timeout 600s;
    proxy_read_timeout 600s;
    
    # Gzip compression
    gzip on;
    gzip_vary on;
    gzip_min_length 1024;
    gzip_proxied expired no-cache no-store private auth;
    gzip_types
        text/plain
        text/css
        text/xml
        text/javascript
        application/javascript
        application/xml+rss
        application/json
        application/xml
        image/svg+xml;

    # Assets files (CSS, JS, images) - HARUS DI ATAS location /
    location /assets/ {
        alias /var/www/hmps/dist/public/assets/;
        expires 1y;
        add_header Cache-Control "public, immutable";
        add_header Vary Accept-Encoding;
    }

    # Attached assets — user-uploaded content that can be overwritten
    location /attached_assets/ {
        alias /var/www/hmps/attached_assets/;
        expires 5m;
        add_header Cache-Control "public, must-revalidate";
        add_header Vary Accept-Encoding;
    }

    # Uploads — user-uploaded content that can be overwritten
    location /uploads/ {
        alias /var/www/hmps/uploads/;
        expires 5m;
        add_header Cache-Control "public, must-revalidate";
        add_header Vary Accept-Encoding;
    }

    # Favicon - HARUS DI ATAS location /
    location = /favicon.ico {
        alias /var/www/hmps/public/favicon.ico;
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    # Favicon PNG
    location = /favicon.png {
        alias /var/www/hmps/public/favicon.png;
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    # Site Web Manifest
    location = /site.webmanifest {
        alias /var/www/hmps/public/site.webmanifest;
        expires 1d;
        add_header Cache-Control "public";
        add_header Content-Type "application/manifest+json";
    }

    # Browser Config
    location = /browserconfig.xml {
        alias /var/www/hmps/public/browserconfig.xml;
        expires 1d;
        add_header Cache-Control "public";
        add_header Content-Type "application/xml";
    }

    # SEO files - FIXED PATHS
    location = /robots.txt {
        alias /var/www/hmps/public/robots.txt;
        expires 1d;
        add_header Cache-Control "public";
        add_header Content-Type "text/plain";
    }

    location = /sitemap.xml {
        proxy_pass http://127.0.0.1:5000/sitemap.xml;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # Security files
    location = /.well-known/security.txt {
        alias /var/www/hmps/public/.well-known/security.txt;
        expires 1d;
        add_header Cache-Control "public";
        add_header Content-Type "text/plain";
    }

    # Health check
    location /health {
        access_log off;
        return 200 "healthy\n";
        add_header Content-Type text/plain;
    }

    # API — potong flood sebelum sampai Node/Mongo (sejajar dengan rate limit di app)
    location /api/ {
        limit_req zone=api burst=18 nodelay;
        limit_conn conn_per_ip 40;
        proxy_pass http://127.0.0.1:5000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }

    # Berita (HTML + data) — sebelumnya tidak dibatasi di edge; sering dipakai untuk flood
    location ~* ^/berita(/|$) {
        limit_req zone=berita_html burst=14 nodelay;
        limit_conn conn_per_ip 28;
        proxy_pass http://127.0.0.1:5000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }

    # Main location (HARUS DI BAWAH)
    location / {
        limit_req zone=site_root burst=55 nodelay;
        limit_conn conn_per_ip 45;
        proxy_pass http://127.0.0.1:5000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;

        # Security
        proxy_hide_header X-Powered-By;
        add_header X-Served-By "HMPS-Server" always;
    }

    # Security: Hide sensitive files
    location ~ /\. {
        deny all;
    }

    location ~ ~$ {
        deny all;
    }

    # Error pages
    error_page 404 /404.html;
    error_page 500 502 503 504 /50x.html;
} 