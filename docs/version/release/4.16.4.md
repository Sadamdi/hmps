# Release v4.16.4 — Auto-deploy reliability (swap, stale dist retry)

| Version | `4.16.4` | Bump | PATCH |

## Summary

Deploy sering stuck: git sudah pull tapi `npm run build` gagal (OOM di VPS 2GB **tanpa swap**), dist lama di-restore, auto-deploy bilang “up to date” dan tidak retry. Patch: swap 2GB, build retry, auto-deploy cek dist stale, `.deploy-built-head` pintar (ops-only tidak rebuild penuh).

## Changed

- `ops/deploy-server.sh` — ensure-swap, build retry, runtime diff untuk dist valid
- `ops/ensure-swap.sh`, `ops/auto-deploy.js`, `ops/install-auto-deploy.sh`
- `/root/auto-deploy.js` — deploy bila runtime stale, retry tiap 30s

## Ops

Di server setelah pull: `bash ops/ensure-swap.sh && bash ops/install-auto-deploy.sh`
