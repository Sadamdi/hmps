# Release v4.14.3 — Nginx CSP fix + official berita import

| Field | Value |
|-------|-------|
| Version | `4.14.3` |
| Status | Current |
| Released | 2026-08-08 |
| Bump type | PATCH |
| Bump reason | Nginx CSP menimpa allowlist embed; import berita prestasi/seminar dari Prodi TI |
| Primary authors | Sulthan Adam Rahmadi (@Sadamdi) |
| Breaking change? | No |

## Summary

CSP ganda dari nginx membuat `embedAllowedHosts` (termasuk wreckit.id) tidak efektif di browser. Nginx tidak lagi mengirim CSP; Node Helmet + settings dinamis yang berlaku. Situs yang menolak framing (mis. wreckit.id `frame-ancestors 'self'`) ditampilkan sebagai kartu tautan. Sembilan berita prestasi/seminar dari situs resmi Prodi TI diimpor sebagai berita medinfo (tanggal diselaraskan).

## Highlights

- Nginx: hapus `Content-Security-Policy` (satu CSP dari app)
- Rich HTML: custom allowlist situs non-viewer → link card
- Import 9 berita resmi TI → medinfo

## Features (detail)

### Embed CSP
- **Source:** `nginx-himatif-encoder.conf`, `rich-html-with-embeds.tsx`, production nginx

### Official news import (ops)
- Sumber: `informatika.uin-malang.ac.id` (kemahasiswaan tidak punya artikel TI langsung yang relevan di scrape awal)
- Skip Wreck-IT (sudah ada)
- Account: medinfo

## User-facing impact

- Hard refresh berita WrecK-IT: tautan wreckit sebagai kartu link (bukan iframe blocked)
- Dashboard berita: 9 entri baru prestasi/seminar

## Fixed

- Allowlist embed “sudah di-add tapi tetap kena CSP nginx”

## Ops

- `_ssh_nginx_fix_csp3.py`, `_import_official_berita.mjs`, `_ssh_set_berita_dates.py`

## Checklist

- [x] Nginx CSP single + wreckit in frame-src
- [x] Import + dates
- [ ] Deploy frontend rich-html
