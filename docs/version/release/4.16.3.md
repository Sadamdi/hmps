# Release v4.16.3 — Deploy: paksa rebuild jika dist stale

| Version | `4.16.3` | Bump | PATCH |
| Released | 2026-08-12 | Breaking | No |

## Summary

Auto-deploy sudah pull git ke HEAD terbaru, tapi bisa skip `npm run build` bila commit tidak mengubah `client/`/`server/` — sementara build sebelumnya gagal dan `restore_dist` mengembalikan bundle lama. Hasil: **git 4.16.2 + dist 4.16.0**. Patch menambah `.deploy-built-head` dan logika `dist_matches_head` agar rebuild wajib jika HEAD ≠ dist yang pernah sukses di-build.

## Changed

- `ops/deploy-server.sh` — `.deploy-built-head`, `HMPS_FORCE_REBUILD=1`
- `.gitignore` — `.deploy-built-head`

## Ops

Production 2026-08-12 ~20:37 WIB: manual force rebuild → `index-CPrJBJ7n.js` live.
