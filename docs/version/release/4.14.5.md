# Release v4.14.5 — Docs, branding, contributors, and agent sync

> Salin dari `version-template.md`. Kontrak API tidak berubah.

| Field | Value |
|-------|-------|
| Version | `4.14.5` |
| Status | Released |
| Released | 2026-08-12 |
| Bump type | PATCH |
| Bump reason | Sinkronisasi docs/branding/ops/agent tanpa breaking API atau fitur user-facing baru |
| Commit range | unit kerja `4.14.5` (lihat commit index / `git log`) |
| Commit count | 1 |
| Diff stats | lihat `git show --stat` setelah commit |
| Primary authors | Sulthan Adam Rahmadi (@Sadamdi) |
| Contributors | Muhammad Alif Mujaddid (@addid-cloud) — tim resmi; tidak ada AI author |
| Related PR / issue | — |
| Related feature docs | `docs/features/feature-summary.md`, `docs/features/02-public-content/04-ssr-sitemap.md` |
| Breaking change? | No |

## Summary

Dokumentasi publik dan agent context tertinggal dari codebase (versi stamp `4.12.0`, contact UIN lama, SOP deploy belum mencerminkan low-RAM `ops/deploy-server.sh` + auto-deploy). Unit kerja ini merapikan branding resmi HIMATIF Encoder, mengunci contributor manusia hanya Adam + Addid, menyelaraskan skills/rules dengan path dan ops aktual, serta menaikkan default fallback `contactEmail` ke `himatif.encoder@gmail.com`. Tidak ada perubahan kontrak JSON API. Grafik GitHub Contributors historis tidak di-rewrite.

## Highlights

- README: hapus paragraf “11 categories”; website `https://himatif-encoder.com`; email `himatif.encoder@gmail.com`
- Fallback `contactEmail` di seed/settings/footer/`db/mongodb.ts` → Gmail resmi
- Living docs (feature-summary, endpoints, master-todo, SOP 07, architecture) stamp `4.14.5` + deploy aktual
- Aturan keras: contributor resmi hanya @Sadamdi dan @addid-cloud
- Skills HMPS + `.cursorrules` sinkron path/ops/graph-fallback

## Features (detail)

### Branding publik + default contact
- **Masalah / tujuan:** README dan fallback app masih memakai domain/email UIN yang bukan kontak resmi platform
- **Perilaku baru:** Contact publik dan default settings menunjuk `himatif-encoder.com` / `himatif.encoder@gmail.com`. Handle Instagram `@himatif.encoder` tidak diubah. Nilai DB production yang sudah diisi admin tidak di-migrate otomatis
- **Siapa terdampak:** public footer (jika settings kosong), dashboard settings placeholder, seed JSON
- **Permission:** N/A (default string)
- **Tenant-aware:** No
- **Source:** `README.md`, `db/mongodb.ts`, `client/src/components/public/footer.tsx`, `client/src/pages/dashboard/settings.tsx`, `update_settings.json`

### Living docs + SOP deploy
- **Masalah / tujuan:** Inventory docs masih `4.12.0`; SOP 07 tidak menjelaskan stop-app → `npm install` → build
- **Perilaku baru:** Stamp `4.14.5`; SOP 07 mendokumentasikan `ops/deploy-server.sh`, `/root/auto-deploy.js`, `pm2-root.service`; feature-summary merujuk `04-ssr-sitemap.md`
- **Siapa terdampak:** developer / ops / AI agent
- **Permission:** N/A
- **Tenant-aware:** No
- **Source:** `docs/SOP/07-deployment.md`, `docs/features/feature-summary.md`, `docs/api/endpoints.md`, `docs/todo/master-todo.md`

### Contributor + agent rules
- **Masalah / tujuan:** Agent bisa menambahkan Claude/Cursor sebagai author; skills typo `pm run check`; path/ops stale
- **Perilaku baru:** AGENTS / `.cursorrules` / docs-maintenance / create-feature melarang AI sebagai contributor. Commit identity: `Sulthan Adam Rahmadi <sultanadamr@gmail.com>`. Graph MCP: pakai dulu, fallback Grep jika server error. Skill `pro-*` tetap guidance umum
- **Siapa terdampak:** AI agent + commit metadata ke depan
- **Permission:** N/A
- **Tenant-aware:** No
- **Source:** `AGENTS.md`, `.cursorrules`, `.agents/skills/**`

## User-facing impact

- Halaman/route yang berubah: footer public (hanya jika `contactEmail` settings kosong); dashboard Settings placeholder email
- Perubahan UX: tidak ada alur baru; teks contact default saja
- Smoke: buka footer → email Gmail jika settings belum diisi; README contact; `/sitemap.xml` tidak berubah perilaku

## Added

- Aturan contributor resmi di `AGENTS.md`, `.cursorrules`, SOP 11, skill `docs-maintenance` / `create-feature`
- Section production deploy aktual di SOP 07

## Changed

- Default `contactEmail`: `hmti@uin-malang.ac.id` → `himatif.encoder@gmail.com` (fallback/seed saja)
- README contact: domain + email resmi; blurb 11 kategori dihapus
- Living docs version stamp: `4.12.0` → `4.14.5`
- Skills checklist: `pm run check` → `npm run check`

## Fixed

- Contact/branding publik tidak konsisten dengan domain produksi `himatif-encoder.com`
- SOP deploy tidak mencerminkan alur low-RAM VPS (root cause: docs stale vs `ops/deploy-server.sh`)

## Security

- N/A untuk auth/permission. Ops docs sengaja tidak memuat secret. Auto-deploy tetap di luar repo (`/root/auto-deploy.js`). Tidak ada rewrite history.

## Ops / Tooling

- Dokumentasi saja: `ops/deploy-server.sh` + media push + PM2 watcher. Perilaku script tidak diubah di unit ini.

## Docs

- `README.md`, `AGENTS.md`, `.cursorrules`
- `docs/README.md`, `docs/SOP/07-deployment.md`, `docs/SOP/11-documentation-maintenance.md`
- `docs/features/feature-summary.md`, `docs/api/endpoints.md`, `docs/todo/master-todo.md`
- `docs/architecture/project-structure.md`
- `.agents/skills/*`
- `docs/version/*`, `docs/openapi.json` `info.version`, `package.json`

## Feature areas touched

- [ ] Auth & Access
- [x] Public Content & CMS
- [ ] Events & Library
- [ ] Organization & Prodi
- [ ] Community Tenant
- [ ] Store / Toko
- [ ] Media & Assets
- [ ] Collaboration / Feedback / Sharing
- [ ] AI Chat & Notifications
- [x] Ops / Security / Monitoring
- [x] Runtime / Docs / Tooling

## API / OpenAPI impact

| Method | Path | Auth / permission | Change | Observed request | Observed response |
|--------|------|-------------------|--------|------------------|-------------------|
| — | — | — | none | — | — |

Hanya bump `info.version` → `4.14.5`. Tidak ada path/method baru.

Wajib sync:

1. `docs/api/endpoints.md` — stamp versi + rujukan `/sitemap.xml` image/video + `/toko` (sudah ada)
2. `docs/openapi.json` `info.version` + `npm run docs:api-html`
3. Feature summary merujuk `04-ssr-sitemap.md`

## Frontend / Backend surface

### Frontend
- Routes/pages: `client/src/pages/dashboard/settings.tsx` (placeholder email)
- Components/hooks/lib: `client/src/components/public/footer.tsx` (fallback email)
- Query keys / invalidation: tidak berubah

### Backend
- Routes: tidak berubah
- Services/storage/models: default field `contactEmail` di `db/mongodb.ts`
- Middleware/cron/sidecar: tidak berubah

## File impact (sample)

| Path | Why relevant |
|------|----------------|
| `README.md` | Branding + hapus blurb stale |
| `db/mongodb.ts` | Default contact email |
| `docs/SOP/07-deployment.md` | Ops aktual |
| `AGENTS.md` / `.cursorrules` | Contributor + graph fallback |
| `.agents/skills/*` | Path/ops/checklist sinkron |

## Data / schema notes

- Hanya default string `contactEmail` di settings schema / seed JSON. Tidak ada migrasi dokumen existing.

## Configuration / env notes

- N/A (tidak ada env baru; nilai secret tidak ditulis)

## Verification

- [x] `npm run check` (dijalankan sebelum commit)
- [ ] Smoke manual footer/settings default email (setelah deploy)
- [ ] Upload/media cleanup — N/A
- [x] `package.json` version = 4.14.5
- [x] `docs/openapi.json` `info.version` = 4.14.5
- [x] `docs/version/versions.md` Current diupdate
- [x] Section baru di `docs/version/changelogs/CHANGELOG.md`
- [x] Feature/API/SOP docs terupdate

## Commit index (lengkap)

| Date | Hash | Author | Subject |
|------|------|--------|---------|
| 2026-08-12 | (isi setelah `git log -1`) | Sulthan Adam Rahmadi | docs: sync branding, contributors, and agent context (4.14.5) |

## Notes / Known gaps

- Grafik Contributors GitHub tetap menampilkan author email commit lama; tidak di-force-push.
- `User-agent: ClaudeBot` di `public/robots.txt` tetap (crawler, bukan contributor).
- Settings production yang sudah diisi admin tidak berubah sampai admin menyimpan ulang.
- `npm run docs:api-html` hanya sinkron `info.version`.

---

## Checklist bump (wajib)

1. PATCH — docs/branding/ops sync, non-breaking.
2. Release note lengkap (file ini).
3. `versions.md` + CHANGELOG updated.
4. `package.json` + OpenAPI synced.
5. Feature/API/SOP/skills updated.
6. Commit + push `origin/main` (auto-deploy server akan pull).
